return {
    doorModelComponentNames = require(script.doorModelComponentNames),
    moduleSettings = require(script.moduleSettings),
    assets = require(script.assets),
    defaultProperties = require(script.defaultProperties)
}