return {
    incorrectAttributeType = "Could not assign attribute `%s` to door because the value is of type `%s` when it is expected to be of type `%s`.",
    missingOrIncorrectComponentOfModel = "The `%s` model is expected to contain an Instance named `%s` of class `%s`.",
}
