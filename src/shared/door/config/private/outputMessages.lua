return {
    incorrectAttributeType = "Could not assign attribute `%s` to door because the value is of type `%s` when it is expected to be of type `%s`.",
    missingOrIncorrectComponentOfModel = "The `%s` model is expected to contain an Instance named `%s` of class `%s`.",
    missingExtensionInitializer = "Extension `%s` is missing it's init function.",
    waitTimeExceededExpectation = "`%d` seconds has elapsed, exceeding limit of `%d` waiting for %s.",
    doorObjectDoesNotExistInEnvironment = "The %s edition of the door object could not be found. Did you initialize this side of the network?"
}
